# 작업지시서: 2024년~2025년 일자별 쇼핑 트렌드 수집 및 확인

본 문서는 네이버 쇼핑인사이트 API를 활용하여 **2024년 1월 1일부터 2025년 12월 31일까지**의 일자별 클릭 추이 데이터를 수집하고 관리하는 계획을 정의합니다.

## 1. 프로젝트 파일 트리 구조
`naverapieda` 폴더 하위의 구성은 다음과 같습니다.

```text
naverapieda/
├── .env                               # API 인증 정보 (Client ID, Secret) 관리
├── instruction_shopping_trend_2025.md # 본 작업지시서 (신규)
├── doc/                               # 네이버 API 관련 공식 문서 정리
│   ├── common_apilist.md
│   ├── datalab_datalab.md
│   ├── datalab_shopping_insight.md
│   └── search_shopping.md
└── data/                              # 수집된 CSV 데이터 저장 폴더
    └── [분야명]_쇼핑트랜드_2024_2025.csv
```

## 2. API 인증 및 보안 관리 (.env)
기존 설정과 동일하게 `.env` 파일을 통해 보안을 관리합니다.

- **파일명**: `.env` (naverapieda 폴더 바로 아래 위치)
- **내용 형식**:
  ```text
  NAVER_CLIENT_ID=발급받은_클라이언트_ID
  NAVER_CLIENT_SECRET=발급받은_클라이언트_시크릿
  ```

## 3. 데이터 수집 기획

### 3.1 수집 대상 및 범위
- **대상 API**: 네이버 데이터랩 쇼핑인사이트 분야별 트렌드 조회 API
- **수집 기간**: **2024-01-01 ~ 2025-12-31 (총 2년)**
- **수집 단위**: 일간 (date)
- **수집 항목**: 카테고리별 일자별 클릭 비중 (ratio)

### 3.2 데이터 저장 규격 (CSV)
- **파일명 규칙**: `[분야명]_쇼핑트랜드_2024_2025.csv`
  - 예: `패션의류_쇼핑트랜드_2024_2025.csv`
- **CSV 컬럼 구성**:
  - `period`: 날짜 (YYYY-MM-DD)
  - `ratio`: 클릭 비중 (해당 기간 내 최대치를 100으로 한 상대값)

## 4. 수행 단계 (Plan)
1. **환경 설정**: 기존 `.env` 파일의 인증 정보가 유효한지 확인합니다.
2. **요청 파라미터 설계**:
   - `startDate`: **"2024-01-01"**
   - `endDate`: **"2025-12-31"**
   - `timeUnit`: **"date"**
3. **데이터 수집 API 호출**: 파이썬 스크립트(`naver_data_collector.py` 등)를 수정하여 위 기간을 적용해 API를 호출합니다.
4. **결과 변환 및 저장**: 응답 데이터를 `data/` 폴더에 `[분야명]_쇼핑트랜드_2024_2025.csv`로 저장합니다.
5. **정합성 확인**: 저장된 CSV 파일이 약 730행(365일 * 2년 + 윤달 등)을 포함하고 있는지 확인합니다.

## 5. 주의 사항
- **조회 기간 제약**: 네이버 API의 단일 요청 최대 기간(예: 2년)을 초과할 경우, 1년 단위로 나누어 요청한 뒤 병합해야 할 수 있습니다. 
- **호출 한도**: 하루 호출 한도(1,000회)를 유의합니다.
- **상대적 지표**: `ratio` 값은 지정된 2년 기간 내 최고치를 100으로 계산한 값입니다.
